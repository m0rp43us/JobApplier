# FastAPI and related dependencies
fastapi
uvicorn[standard]

# PostgreSQL dependencies
psycopg2-binary
sqlalchemy

# For AI helper
openai  # Assuming you're using OpenAI's GPT models, adjust if using another library
llama-index  # If you use Ollama or another LLM library, replace or add as necessary

# For web scraping (if used)
beautifulsoup4
requests

# For interacting with your web bots (if used)
selenium
webdriver-manager

# Other utility packages
pydantic

# Optional: For JSON Web Tokens (if using for authentication)
pyjwt

# Optional: For data analysis and manipulation (if using)
pandas

# Logging
loguru
